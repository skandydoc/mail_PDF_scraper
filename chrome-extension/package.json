{
  "name": "gmail-pdf-processor",
  "version": "1.0.0",
  "description": "Chrome extension for processing PDF attachments from Gmail",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p dist && zip -r dist/chrome-extension.zip . -x '*.git*' -x 'node_modules/*' -x 'dist/*' -x '*.DS_Store'"
  },
  "keywords": [
    "chrome-extension",
    "gmail",
    "pdf",
    "attachment",
    "processor"
  ],
  "author": "",
  "license": "CC-BY-SA-NC-4.0",
  "devDependencies": {
    "eslint": "^8.56.0",
    "eslint-config-google": "^0.14.0"
  }
} 